sudo: required

services:
  - docker

language: java
dist: trusty

jdk:
- openjdk8

cache:
  directories:
  - $HOME/.m2

install:
- mvn clean package -U

after_success:
- docker login -u="janerz6" -p="dockerRSO"
- docker build -t janerz6/accommodations .
- docker create janerz6/accommodations -name=accommodations_container
- docker commit accommodations_container janerz6/accommodations
- docker push janerz6/accommodations